# TURTLE SOUP v1.9 · AI 语言桥与全前端重构 RC

状态：`internal-rc / human-evaluation-pending`

v1.9 覆盖 C01–C60，保持 C01–C60 的真相图、canonical hash、证明证书、胜负逻辑和 `SaveEnvelope.schemaVersion: 1` 不变。v1.8 HEAD 基线为 `bb4c81b7d399037693c059d1ada481ca898cf368`，v1.8 ZIP SHA-256 继续为 `43b6dff6b70abec6a74788bb28fba6f7301eb91dd892818584c82f48fc88d30d`。

## 本轮交付

- 统一 `InvestigationShell`：桌面主工作区 + 调查本，手机 `现场 / 提问 / 证据 / 推断`。
- 直接中文的现场、提问、证据和证明反馈；保留“系统理解为”与确定性事实回答的分层。
- 可选 BYOK AI 语言桥：只接收公开谜面、原问题和临时候选 token；玩家确认后才转为可重放命令。AI 关闭、无 Key、离线、超时、CORS、非法 JSON 或恶意输出时回退确定性流程。
- 证据检查、撤销最近问题、玩家本地“我的真相”、挑战轮换、本地聚合诊断和体验问题临时导出。
- 首页改为最近调查置顶、单季切换、首屏可行动；不再一次展开 60 案长列表。
- WebKit/Firefox 移动端长选项溢出、弹层焦点恢复、离线 Worker 等待和错误截图流程已修复。

## 自动化验证

- `npm run check`：83/83 通过。
- 构建：63 个静态页面，60 个案件路由；离线预缓存 854 条，约 6.38 MB。
- 三浏览器：Chromium、Firefox、WebKit；桌面 1440×900、手机 390×844；360 条路由、42 条代表流程、25 张实际截图；控制台错误 0，离线恢复通过，serious/critical axe 为 0。
- 存档：60/60 旧命令与投影重放通过，schema 仍为 1。
- 安全：60/60 AI 公开上下文扫描通过，无静态真相泄漏、远程追踪或 v1.8 工件变化。
- 性能：静态目录 6,422,007 字节；初始 JavaScript 646,922 字节；最大文件 391,291 字节。

## 运行

```text
$env:TURTLE_SOUP_RELEASE_PROFILE='v1.9-internal-rc'
npm run check
npm run build
npm run audit:v19:browsers
npm run audit:v19:save
npm run scan:v19
npm run audit:v19:performance
npm run package:v19
npm run verify:v19
```

发行包：`dist/turtle-soup-v1.9-internal-rc-web-pwa.zip`

自动化结果只证明一致性、公平性下界、可达性、浏览器可操作性、离线恢复、性能和反泄漏；真人参与数为 0，不能宣称 Fun Gate、市场验证或“已证明好玩”。
