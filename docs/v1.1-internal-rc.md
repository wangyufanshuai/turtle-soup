# TURTLE SOUP v1.1 Internal RC

状态：`internal-rc / human-evaluation-pending`

本版本在 v1.0 双季内部 RC 的基础上加入第三季 C25–C36，并优化 36 案共享界面、视觉资产、推理板、音景、离线缓存和可选本地主持表达层。

## 已完成

- Season 1 C01–C12 canonical 文件 hash 保持冻结，旧 `SaveEnvelope` schema 继续兼容。
- Season 2 C13–C24 真相图与证明逻辑未被修改。
- Season 3 C25–C36：12 案、2,840 条中文语料、两套最小证明、双推理板；C33–C36 含双章节。
- 三季总计 36 案、7,548 条问题语料。
- 本地主持默认关闭，只接收公开 `PresentationContext`；localhost 端点、2 秒超时、输出 schema/极性/内部标记/未公开数值校验和确定性回退均已实现。
- 252 个 WebP 资产覆盖 36 案桌面主场景、手机裁切和证据局部；原始生成母版不进入发布目录，provenance 记录见 `raster-manifest.v1.1.json`。
- 推理板按状态、空间、来源、身份、测量、采样和聚合关系提供不同操作语义。

## 自动化验证

- 核心、运行时、Season 2、Season 3 和本地主持测试：53/53。
- 36/36 案件质量、引用、反泄漏、两套证明和回放门禁通过。
- 合成玩家：36 案 × 8 persona = 288/288。
- 浏览器：Chromium / Firefox / WebKit 桌面 108/108 路径；手机 Season 3 12/12；离线恢复通过。
- axe serious/critical：0。
- 静态目录：4,896,984 bytes；初始 JavaScript：615,018 bytes；无 source map。
- v1.1 ZIP：`dist/turtle-soup-v1.1-internal-rc-web-pwa.zip`。

## 明确未验证

自动化结果只证明确定性一致性、公平性下界、可达性、反泄漏和工程性能；真人理解率、乐趣、节奏、审美偏好、音频接受度、市场竞争力和低端移动设备表现仍未验证。真人参与数保持 `0`，Fun Gate 保持 `pending`。
