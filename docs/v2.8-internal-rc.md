# TURTLE SOUP v2.8 Guided Investigation Internal RC

Status: `internal-rc / human-evaluation-pending`

- Cases: 84 across five seasons.
- Formal Fun Gate participants: 0.
- Founder exploratory sessions: 1.
- Save schema: 1.
- External deployment: not performed.

## Scope

v2.8 keeps every C01–C84 truth graph, fact, event, proof certificate, canonical hash and win condition frozen. It reduces presentation competition around the first investigation and makes secondary controls progressively discoverable.

- The separate global feedback strip is merged into the single next-action card, leaving one guidance surface above the four workspaces.
- Settings are divided into accessible `体验 / AI / 数据` tabs instead of one long scrolling form.
- Ordinary accessibility controls open first; optional AI and testing tools no longer compete with basic settings.
- Host wording controls are treated as an advanced disclosure because they never affect truth or question routing.
- Release registry overlay selection now follows version capability boundaries instead of adding each new profile to repeated hard-coded arrays.
- Static release reporting derives its versioned report path generically while retaining explicit historical cache identities.

## Compatibility and safety

- C01–C84 identities remain byte-for-byte inherited from v2.7.
- `SaveEnvelope.schemaVersion` remains `1`; deterministic command logs replay to identical projections.
- Unknown and cross-case saves fail closed.
- AI remains optional and presentation-only. Localhost endpoints can operate without a Key; remote endpoints require HTTPS and a session-only Key.
- API keys do not enter localStorage, IndexedDB, saves, diagnostics, Worker messages or exports.

## Verification boundary

Automated checks cover deterministic tests, 84-case routing, representative full flows, desktop/mobile settings navigation, offline recovery, accessibility blockers, static budgets, save replay and hidden-data scans. They do not establish fun, comprehension, pacing, retention, aesthetic preference or market fit. Formal unknown-answer human evaluation is still pending.
