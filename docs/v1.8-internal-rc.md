# TURTLE SOUP v1.8 · Golden Nine Experience Hardening

状态：`internal-rc / human-evaluation-pending`  
真人参与：`0`  
首发语言：中文；核心不依赖网络、API Key、模型或账号。

## 本轮目标

v1.8 不新增案件，也不修改 C01–C60 的真相图、事实、事件、证明证书、canonical hash、胜负逻辑或旧存档协议。改动集中在黄金九案：

`C01 → C03 → C13 → C06 → C17 → C24 → C33 → C48 → C60`

新增公开投影驱动的调查推进条、案件专属前三拍与结案回报、证据保留/接入/暂时搁置取舍、C33/C48/C60 的“当前板 / 事件链”单任务切换，以及不接触隐藏事实的持续推进代理。

## 自动化结果

- `npm run check`：73/73 通过。
- 60 案 × 12 类合成玩家 = 720 次推进代理运行；无隐藏字段泄漏。
- Chromium、Firefox、WebKit：60 案路由、6 组完整流程、110 张实际截图；控制台错误 0。
- Chromium 黄金九案校准：桌面 1440×900、手机 390×844 均通过；离线恢复通过。
- serious/critical axe：0；横向溢出、底部遮挡和输入失焦均为 0。
- 静态目录约 6.63 MB；首始 JavaScript 653,711 字节；最大文件 389,209 字节。
- v1.0–v1.7 ZIP SHA-256 保持不变；v1.8 ZIP 独立生成。

## 重要边界

这些结果只证明确定性一致性、公平性下界、可达性、公开操作推进信号、浏览器可操作性、离线恢复、性能和反泄漏。它们不能证明真人理解率、乐趣、审美、节奏、留存、市场竞争力或 Fun Gate 通过。

## 运行

```text
npm run check
npm run build
npm run audit:v18:momentum
npm run audit:v18:browsers
npm run audit:v18:visual
npm run audit:v18:performance
npm run scan:v18
npm run package:v18
npm run verify:v18
```

发行包：`dist/turtle-soup-v1.8-internal-rc-web-pwa.zip`
