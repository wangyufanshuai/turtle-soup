# TURTLE SOUP v2.6 · Investigation Workbench Internal RC

状态：`internal-rc / human-evaluation-pending`。正式 Fun Gate 参与人数为 `0`；创始人探索会话为 `1`。

本版本不新增案件，也不修改 C01–C84 的真相、事件、事实、证明证书、canonical hash、胜负逻辑或 schema-one 存档。改动集中在统一调查工作台、中文操作语义、推理板直接交互、案件搜索、首屏导航、现场题面优先级和移动布局。

验证命令：

```powershell
$env:TURTLE_SOUP_RELEASE_PROFILE='v2.6-internal-rc'
npm run check
npm run build
npm run package:v26
npm run audit:v26:performance
npm run scan:v26
npm run audit:v26:save
npm run audit:v26:experience
npm run audit:v26:content-difference
npm run audit:v26:browsers
npm run verify:v26
```

自动化报告只证明一致性、可达性、反泄漏、离线资源和工程门禁，不替代真人对乐趣、节奏、审美、理解率、留存或市场适配的判断。未授权外部部署，不包含账号、分析、云存档或远程 AI 依赖。
