# TURTLE SOUP v1.5 · Golden First Hour RC

状态：`internal-rc / human-evaluation-pending`  
真人参与：`0`

v1.5 不增加案件，也不修改 C01–C60 的事件、事实、证据关系、证明证书、canonical hash、胜负逻辑或 SaveEnvelope schema。它把现有内容集中为十二个黄金体验样板：C01、C03、C06、C13、C17、C24、C25、C33、C36、C37、C48、C60。

## 本轮变化

- C01 前 90 秒渐进披露：开场只保留现场与第一问；回答或现场检查后打开证据架，两条回答或两件已检查证据后打开推理板。
- 共享证据聚焦层：分页证据架、模态检查、Escape、焦点锁定/返回、图片失败文字降级与手机安全区。
- 直接操作推理板：事件片、可视槽位、排序、节点选择和关系连接仍全部转换为既有 GameCommand。
- 黄金十二案拥有独立调查节奏、证据检查语言、顿悟节点、回放音调和至少五种 DOM/SVG 证据片；图像、颜色和声音均不承载唯一线索。
- v1.5 QuestionAliasPack 只把口语、省略、代词、错字、否定和时间限定路由到既有公开查询；hash、查询引用或别名冲突时整体安全回退。
- 第一季问题语料删除仅改标点/空格的重复模板，C01–C12 每案不少于 150 条且规范化重复率低于 10%。
- v1.4 已在干净主源码路径重建，855/855 个静态文件与归档目录逐字节一致；另路径构建仍受 Webpack 绝对 context 影响，因此历史 ZIP 继续以固定 SHA-256 保护。

## 验证命令

```text
TURTLE_SOUP_RELEASE_PROFILE=v1.5-internal-rc npm run check
TURTLE_SOUP_RELEASE_PROFILE=v1.5-internal-rc npm run build
npm run audit:v15:language
npm run audit:v15:browsers
npm run audit:v15:experience
npm run audit:v15:visual
npm run audit:v15:performance
npm run scan:v15
npm run package:v15
npm run verify:v15
```

## 明确限制

自动化只证明一致性、公平性下界、可达性、兼容性和工程稳定性。它没有证明乐趣、叙事感染力、审美偏好、真人理解率或市场适配。正式发布仍等待冻结 C01 正式 Fun Gate，以及 C02–C60 的分层公平性抽测。
