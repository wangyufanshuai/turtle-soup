# TURTLE SOUP v0.3 发布候选记录

验证日期：2026-08-22

## 本轮决策

真人测试不再作为开发阶段阻塞条件。本轮目标是先完成内部发布候选；真人评测仍保留为正式公开发布前的最终验证。

## 完成项

- 三案共享确定性核心、Worker、IndexedDB 存档和 PWA 缓存。
- C02/C03 增加错误理论无效化、证据顺序收敛和反泄漏测试。
- 不兼容或跨版本存档会被安全拒绝，并在界面明确提示，不会静默恢复。
- C02/C03 统一显示离线状态、本地保存状态、驱动条件、时间链上下移动和结案 debrief。
- 三案桌面和手机布局完成浏览器冒烟检查。

## 验证结果

- `npm run check`：30/30 测试通过。
- `npm run validate:cases`：三案无 error，查询覆盖、证明回放、替代理论、必要证据覆盖均为 100%。
- `npm run build`：静态构建通过。
- 静态首页、三条案件路由、manifest 和 service worker 均返回 200。
- 1440×900 桌面：三案布局和主要调查动作可用。
- 390×844 手机：C02/C03 无横向溢出，底部案件导航可切换。
- 浏览器 console：本轮未观察到 error/warn。

## 尚未宣称的内容

这仍是内部发布候选，不是已经被市场验证的“顶级成品”。真人玩家尚未验证理解率、证明满足感、难度和长期重玩价值；正式发布前必须完成最终真人评测。

