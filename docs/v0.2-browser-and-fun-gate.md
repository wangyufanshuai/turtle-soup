# TURTLE SOUP v0.2 验证记录

检索/验证日期：2026-08-22

## 自动验证

- `npm run check`：29/29 核心测试通过。
- `npm run validate:cases`：C01、C02、C03 均无 error；查询覆盖 100%，证明回放覆盖 100%，替代理论覆盖 100%，必要证据覆盖 100%。
- C02/C03 中文问题语料：分别 152、162 条；归一化测试全部稳定匹配。
- `npm run build`：Next.js 静态构建通过，生成首页和三条案件路由。

## 浏览器冒烟

使用 Game Studio browser playtest 流程在本地开发服务完成：

- 案件选择页：三案标题、难度、时长、标签和独立场景均可见。
- C02 桌面：雪夜路线场景、物理路线右栏、提问和证据检查可操作。
- C03 桌面：剧院后台场景、身份档案右栏、驱动条件选择和时间链操作可见。
- C03 手机 390×844：底部“后台 / 证词 / 身份链”导航可切换；页面宽度无横向溢出（375 CSS px 内容宽度）。
- C01 Fun Gate 面板：匿名 tester ID、问题计数、提示标记、JSON/CSV 本地导出入口可见。
- 冒烟期间未观察到 console error/warn。

## Fun Gate 状态

C01 真人 Fun Gate 尚未完成，因此不能宣称发布门槛通过。当前实现只提供本地会话记录和导出能力，不上传远程分析；导出内容是聚合指标，不包含真相、隐藏事实、内部事件 ID 或证明证书。

下一步是招募 10–15 名不知道答案的目标玩家，按产品圣经中的理解率、证明率、完成率和满足感门槛记录结果，再决定是否冻结共享交互并进入三案内部盲测。

