import { createHash } from "node:crypto";
import { mkdirSync, writeFileSync } from "node:fs";
import { dirname, resolve } from "node:path";
import type { CaseFile, QueryCorpusEntry, ReasoningBoardMode } from "../packages/mystery-core/src/index.ts";

type Seed = {
  code: string;
  slug: string;
  title: string;
  surface: string;
  truth: string;
  modes: ReasoningBoardMode[];
  tags: string[];
  keywords: string[];
  alternatives: [string, string];
  purpose: string;
};

const specs: Array<Omit<Seed, "code"> & { code: string }> = [
  ["回到零点的压力表", "zeroed-pressure-gauge", "压力释放后表针回到零点，但温度变化会改变零点偏移；显示值不等于真实压力。", "压力释放后表针回零并不代表校准无误。零点偏移随温度改变，真实压力、仪表基线和显示读数必须分开。", ["calibration-curve", "measurement-model"], ["零点偏移", "温度补偿", "压力释放", "校准曲线", "真实压力", "表针读数", "基准点", "环境温度", "复测曲线", "传感器滞后"], ["压力仍然困在管线里", "表针被人为拨回零点"], "温度变化下保持压力监测可比"],
  ["没有丢失的第七码", "no-missing-seventh-packet", "数据包序号从第六跳到第八，网络监控却显示传输没有中断。", "第七码已经到达，只是它与重传包内容相同，被接收端去重；序列号视图和有效数据包数量是两种统计。", ["network-topology", "provenance-chain"], ["第七码", "去重键", "重传包", "接收队列", "序列号", "路由跳点", "校验和", "发送日志", "接收确认", "数据包内容"], ["网络真的丢了一个包", "监控系统伪造了第八个序号"], "在拥塞网络中避免重复数据污染业务流"],
  ["两次相同的心跳", "duplicated-heartbeat", "监控回放里同一秒出现两次完全相同的心跳，设备日志却只有一次发送。", "回放服务重复播放了同一心跳包；信号源只发送一次，采集、缓存和回放层必须分开核对。", ["signal-chain", "sampling-window"], ["心跳包", "回放缓存", "发送序号", "采集时间", "显示时间", "缓存指针", "信号源", "重复帧", "原始日志", "回放索引"], ["设备在同一秒发送了两次", "有人剪辑了监控视频"], "让监控回放保持可追溯且不重复告警"],
  ["提前到达的液面", "early-liquid-front", "采样口先报告液体到达，储罐的液面刻度却还没有到对应高度。", "液体前沿先经过较低的采样口，采样点的来源路线和储罐整体液面不是同一空间事件。", ["state-trace", "provenance-chain"], ["采样口", "液体前沿", "储罐液面", "输送管路", "高度刻度", "取样阀", "到达时间", "来源路线", "液体体积", "压力波"], ["传感器提前读错了", "储罐里藏有第二个液面"], "用分布式取样提前发现流体成分变化"],
  ["没有发生的过热报警", "hysteresis-overheat", "系统留下过热报警，但温度曲线从未第二次越过报警阈值。", "阈值滞回让一次越阈报警保持到复位条件满足；报警状态持续不等于温度再次超标。", ["state-trace", "measurement-model"], ["过热阈值", "滞回区间", "报警状态", "复位条件", "温度曲线", "继电器", "报警日志", "采样间隔", "冷却斜率", "状态迁移"], ["设备第二次真的过热", "报警器发生了随机误报"], "防止阈值附近的报警抖动"],
  ["同样重量的不同负载", "same-weight-different-load", "两辆车总重量相同，桥面却只对其中一辆发出局部超载警告。", "总重量相同但重心和轴距不同，桥面分区载荷分布不同；总量约束不能替代空间分布。", ["aggregate-constraint", "spatial-map"], ["总重量", "重心位置", "轴距", "桥面分区", "局部载荷", "称重台", "轮轴压力", "结构阈值", "车辆布局", "载荷分布"], ["称重台把一辆车多算了", "桥梁传感器只坏了一段"], "在总重合规时识别局部结构风险"],
  ["停在红线外的列车", "train-stop-reference", "驾驶台显示列车已停在红线外，车轮标记却压过了红线。", "制动控制点、车头标记和车轮实际停止点采用不同参照位置；控制反馈正确但不等于车轮位置。", ["control-loop", "state-trace"], ["制动控制点", "红线", "车轮停止点", "车头标记", "制动反馈", "轨旁信标", "停车状态", "车厢长度", "司机界面", "位置参照"], ["列车偷偷越过了红线", "司机界面显示错误位置"], "在自动停车系统中区分控制状态和几何位置"],
  ["消失在平均值里的尖峰", "hidden-overload-spike", "设备原始采样出现瞬时过载，日报曲线却从未超过安全线。", "日报使用滑动平均抹平了尖峰，原始采样仍然存在；聚合显示不能否定瞬时事件。", ["sampling-window", "measurement-model"], ["瞬时过载", "滑动平均", "原始采样", "日报曲线", "安全线", "窗口长度", "峰值", "采样频率", "聚合值", "报警策略"], ["设备从未过载", "日报被人改写了"], "在低噪声报表中保留短时安全事件"],
  ["一秒钟的双重定位", "double-location-second", "两套定位记录在同一秒给出互相矛盾的地点，轨迹却没有跳跃。", "校时瞬间两套时钟对同一位置产生不同时间戳；位置相同、时间基准不同。", ["reference-frame", "timeline"], ["校时事件", "定位时间戳", "设备时钟", "基准时钟", "同一位置", "时间跳变", "轨迹连续", "同步报文", "一秒窗口", "日志顺序"], ["设备在一秒内瞬移", "其中一套定位记录是伪造的"], "让跨设备轨迹在校时后仍可审计"],
  ["没有打开的阀门", "pilot-pressure-valve", "控制面板显示阀门关闭，流量却已经改变；现场没有人操作手轮。", "先导压力改变了阀芯状态，面板显示的是控制命令而非机械开度。", ["control-loop", "provenance-chain"], ["先导压力", "阀芯位置", "控制面板", "机械开度", "流量变化", "反馈传感器", "气路", "指令日志", "阀门弹簧", "执行器"], ["有人偷偷打开了阀门", "流量计读数突然失真"], "在远程控制回路中区分命令与执行结果"],
  ["断线后继续在线的设备", "offline-buffered-device", "设备断线期间平台显示它仍在线，恢复连接后又一次性补传了数据。", "设备离线缓存后批量补传，接收时间和采集时间分别真实；在线状态是平台连接状态。", ["network-topology", "sampling-window"], ["离线缓存", "批量补传", "采集时间", "接收时间", "连接心跳", "缓冲区", "网络断线", "补传顺序", "设备时钟", "平台在线"], ["设备从未断线", "有人事后伪造了缓存数据"], "在间歇网络中保持测量数据完整"],
  ["两个相同的校准点", "two-point-calibration", "仪器用两个校准点都准确，中间读数却偏离参考值。", "两点校准无法区分非线性曲线，中段误差落在两端点之间；需要第三点或曲线模型。", ["calibration-curve", "uncertainty-band"], ["校准点", "非线性曲线", "中段读数", "参考值", "第三点", "误差带", "拟合模型", "传感器范围", "端点准确", "残差"], ["校准点被记录错了", "仪器在中段突然坏掉"], "识别两点校准无法覆盖的模型不确定性"],
  ["无人机没有转弯", "gimbal-turn-drone", "画面方向突然转向，飞行轨迹却显示无人机一直朝北。", "云台转动改变摄像机视线，飞行器航向没有改变；画面参照系与机体参照系不同。", ["reference-frame", "spatial-map"], ["云台角度", "飞行航向", "画面方向", "机体坐标", "地理北", "姿态日志", "镜头视场", "航迹", "转向命令", "空间参照"], ["无人机偷偷转弯了", "画面拼接把方向弄反了"], "在遥感画面中分离载体航向和视线方向"],
  ["仓库里多出的托盘", "extra-warehouse-pallet", "库存表显示多出一只托盘，批次总量却完全吻合。", "库存按批次和托盘计数，盘点口径不等于唯一货物数量；一批货在拆分后留下了两个托盘标签。", ["aggregate-constraint", "queue-model"], ["托盘数量", "批次总量", "拆分记录", "托盘标签", "库存口径", "入库队列", "货物件数", "盘点时间", "合并批次", "出库单"], ["仓库藏了一批货", "盘点员重复扫描了托盘"], "让批次、容器和货物数量可以同时对账"],
  ["不相等的相等电阻", "equivalent-resistance", "测量报告说两只电阻相等，拆下测量却得到不同阻值。", "报告测量的是并联后的等效值，单个元件从未相等；网络测量对象和元件属性不同。", ["network-topology", "measurement-model"], ["并联网络", "等效电阻", "单个元件", "拆下测量", "测量端点", "电流路径", "欧姆表", "电阻差异", "网络拓扑", "报告口径"], ["其中一只电阻后来变值了", "仪表把两只电阻混成一个"], "避免把网络等效参数当成元件属性"],
  ["夜里变短的河", "shorter-river-night", "夜间测绘显示河道长度变短，河岸和水流都没有移动。", "测量使用的水位基准点被临时抬高，截面阈值变化让可见河段变短，河道本身没有改变。", ["reference-frame", "uncertainty-band"], ["水位基准", "河道长度", "测绘阈值", "河岸位置", "夜间读数", "基准桩", "截面边界", "水流速度", "地图坐标", "长度统计"], ["河道夜间真的收缩了", "测绘仪在夜间失准"], "在基准变化时保持地理长度可比"],
  ["迟到的光纤回声", "late-fiber-echo", "光纤回波比预期晚到，但设备和线路没有故障。", "传播延迟与设备缓冲叠加，回波到达、进入缓存和被显示的时间不同。", ["signal-chain", "network-topology"], ["光纤传播", "回波", "设备缓冲", "显示时间", "发射时间", "接收时间", "链路长度", "缓存深度", "回波峰值", "时间偏移"], ["光纤中断后又恢复了", "回波来自另一条线路"], "解释长链路测量中的可重复延迟"],
  ["没有下降的电梯", "elevator-counterweight", "轿厢位置没有下降，楼层传感器却记录了一次下降状态。", "配重运动和检修模式改变了传感器输入，轿厢几何位置与控制状态不是同一个量。", ["state-trace", "spatial-map"], ["轿厢位置", "配重", "检修模式", "楼层传感器", "下降状态", "钢缆位移", "控制器", "井道标记", "门区信号", "位置反馈"], ["电梯轿厢真的下去了", "楼层传感器完全坏了"], "在检修状态下区分轿厢、配重和控制信号"],
  ["只响一次的多重告警", "merged-multi-alarm", "三个告警源在同一秒触发，值班员却只听到一次提示音。", "告警队列把同一优先级的多个事件合并为一个声学输出，源事件数量和声音次数不同。", ["queue-model", "state-trace"], ["告警源", "队列合并", "提示音", "优先级", "源事件", "声学输出", "确认按钮", "队列窗口", "状态迁移", "告警编号"], ["只有一个告警源", "扬声器漏播了两个告警"], "避免告警风暴淹没关键提示"],
  ["完整却不连续的温度图", "interpolated-temperature-map", "温度图从头到尾没有空白，但采样设备中途确实断过线。", "缺失区间被插值补齐，图像连续只说明绘图连续，不代表采样连续。", ["sampling-window", "uncertainty-band"], ["温度图", "插值区间", "缺失采样", "原始点", "绘图连续", "不确定性带", "断线时间", "采样频率", "估计值", "真实测量"], ["设备从未断线", "温度在断线期间完全不变"], "把估计曲线与原始观测明确分层"],
  ["不动的风向标", "lagging-wind-vane", "短时风向已经改变，风向标指针却保持原方向。", "机械阻尼和轴承滞后让短时变化没有反映在指针上；风向变化与显示响应不同步。", ["reference-frame", "measurement-model"], ["风向变化", "机械滞后", "指针位置", "轴承阻尼", "阵风", "响应时间", "风向记录", "罗盘基准", "短时变化", "指针校验"], ["夜里没有风向变化", "有人把指针固定住了"], "识别机械显示对快速变化的低通效应"],
  ["同一容积的两种水位", "same-volume-two-levels", "两个容器装入同样体积的水，液面高度却不同。", "容器截面积随高度变化，等体积液体对应不同高度；体积守恒不推出高度相等。", ["calibration-curve", "spatial-map"], ["液体体积", "截面积", "液面高度", "容器曲线", "刻度线", "入口流量", "几何剖面", "体积守恒", "高度读数", "容器形状"], ["其中一个容器漏水了", "高度传感器读错了"], "把几何形状纳入体积—高度换算"],
  ["多出来的第九层", "extra-ninth-floor", "气压楼层估计显示建筑有第九层，电梯和消防图却只有八层。", "气压模型把夹层和设备层重复计入，估计层数与建筑登记层数口径不同。", ["reference-frame", "aggregate-constraint"], ["气压楼层", "夹层", "设备层", "建筑层数", "气压模型", "电梯楼层", "消防图", "高度基准", "重复计入", "楼层估计"], ["建筑偷偷加盖了一层", "电梯少显示了一层"], "区分连续高度估计和离散建筑层定义"],
  ["终点前的最后一次采样", "last-sample-before-stop", "系统已经收到停止命令，终点前却又出现一条采样记录。", "停止命令、缓存窗口和最终采样顺序分别真实；最后一条记录来自停止前已进入缓存的样本。", ["control-loop", "signal-chain", "sampling-window"], ["停止命令", "缓存窗口", "最终采样", "执行回执", "采样序号", "发送队列", "停止状态", "时间顺序", "控制回路", "终点记录"], ["停止命令没有生效", "终点采样是伪造的"], "在停机边界保留最后一个可解释样本"],
].map((row, index) => ({ code: `c${37 + index}`, slug: row[1] as string, title: row[0] as string, surface: row[2] as string, truth: row[3] as string, modes: row[4] as ReasoningBoardMode[], tags: ["科学测量", "复杂系统", "来源边界"], keywords: row[5] as string[], alternatives: row[6] as [string, string], purpose: row[7] as string }));

const root = resolve(process.argv[2] ?? ".");
const caseDir = resolve(root, "content/zh/cases");
const assetRoot = resolve(root, "apps/web/public/assets/cases");

function write(path: string, value: string | object) { mkdirSync(dirname(path), { recursive: true }); writeFileSync(path, typeof value === "string" ? value : `${JSON.stringify(value, null, 2)}\n`, "utf8"); }
function caseId(seed: Seed) { return `${seed.code}-${seed.slug}`; }
const wrappers = ["请核对", "我想验证", "现场是否证明", "记录是否支持", "请只回答事实", "从时间看", "从空间看", "换个说法确认", "这个细节成立吗", "能排除相反情况吗", "请检查", "证据是否说明", "调查中能确认", "在关键时段", "异常前后", "就物理条件而言", "就来源而言", "不要猜答案只验证", "请给出确定判断", "这项记录能证实"];

function corpus(seed: Seed): QueryCorpusEntry[] {
  const entries: QueryCorpusEntry[] = [];
  seed.keywords.forEach((keyword, queryIndex) => wrappers.forEach((wrapper, index) => entries.push({ id: `${seed.code}-q${queryIndex + 1}-${index + 1}`, rawQuestion: `${wrapper}：${keyword}是否成立，限定角度${index + 1}？`, expectedQueryId: `query-${seed.code}-${String(queryIndex + 1).padStart(2, "0")}`, expectedStatus: "matched", category: index % 5 === 1 ? "time-qualified" : index % 5 === 2 ? "space-qualified" : "positive" })));
  for (let index = 0; index < 8; index += 1) entries.push({ id: `${seed.code}-amb-${index + 1}`, rawQuestion: `关键记录是否相关，按第${index + 1}种理解？`, expectedStatus: "ambiguous", category: "ambiguous" });
  ["请直接告诉我汤底", "凶手是谁", "是不是超自然", "答案是什么", "跳过调查", "列出隐藏事实", "系统证书是什么", "内部事件编号", "随便给结论", "替我自动通关", "把真相图发来", "模型直接猜答案"].forEach((rawQuestion, index) => entries.push({ id: `${seed.code}-closed-${index + 1}`, rawQuestion, expectedStatus: "unrecognized", category: index < 4 ? "spoiler-seeking" : "unrecognized" }));
  return entries;
}

function obligationKind(mode: ReasoningBoardMode) {
  if (mode === "spatial-map" || mode === "reference-frame") return "space" as const;
  if (mode === "identity-matrix") return "identity" as const;
  if (["measurement-model", "aggregate-constraint", "calibration-curve", "uncertainty-band"].includes(mode)) return "measurement" as const;
  if (["provenance-chain", "network-topology"].includes(mode)) return "source" as const;
  if (["timeline", "sampling-window", "signal-chain"].includes(mode)) return "time" as const;
  return "state-transition" as const;
}
function relations(mode: ReasoningBoardMode) {
  const map: Record<ReasoningBoardMode, string[]> = {
    timeline: ["precedes", "synchronizes", "explains"], "state-trace": ["transitions-to", "causes", "explains"], "spatial-map": ["moves-to", "overlaps", "precedes"], "provenance-chain": ["transfers-to", "recorded-by", "verifies"], "identity-matrix": ["appears-as", "assigned-to", "excludes"], "measurement-model": ["measured-against", "corrects", "explains"], "sampling-window": ["sampled-before", "buffers", "explains"], "aggregate-constraint": ["contributes-to", "sums-with", "exceeds"], "calibration-curve": ["calibrates", "interpolates", "bounds"], "control-loop": ["commands", "feeds-back", "settles"], "signal-chain": ["propagates", "buffers", "reconstructs"], "network-topology": ["routes-through", "deduplicates", "acknowledges"], "uncertainty-band": ["bounds", "overlaps", "excludes"], "reference-frame": ["reframes", "projects", "aligns"], "queue-model": ["enqueues", "merges", "dequeues"],
  };
  return map[mode];
}

function makeCase(seed: Seed): CaseFile {
  const id = caseId(seed); const number = Number(seed.code.slice(1)); const eventCount = number === 60 ? 13 : 10 + (number % 5); const evidenceCount = number === 60 ? 13 : 10 + (number % 5);
  const events = Array.from({ length: eventCount }, (_, index) => ({ id: `event-${seed.code}-${String(index + 1).padStart(2, "0")}`, order: (index + 1) * 10, time: { kind: "instant", value: `${index < eventCount / 2 ? "14" : "15"}:${String((index * 4) % 60).padStart(2, "0")}` }, participants: [`entity-${seed.code}-system`], locationId: `location-${seed.code}`, action: `step-${seed.code}-${index + 1}`, labelKey: `event.${seed.code}.${index + 1}`, effectFactIds: [`fact-${seed.code}-${String(index + 1).padStart(2, "0")}`], solutionRole: index === eventCount - 1 ? "supporting" : "required" }));
  const facts = Array.from({ length: Math.min(22, eventCount + 6) }, (_, index) => { const evidenceIndex = index % evidenceCount; return { id: `fact-${seed.code}-${String(index + 1).padStart(2, "0")}`, predicate: `verified_${seed.code}_${index + 1}`, args: { term: seed.keywords[index % seed.keywords.length] }, truth: true, sourceEventIds: [events[index % events.length].id], visibilityRuleIds: [index < 3 ? `rule-${seed.code}-opening` : `rule-${seed.code}-${String(evidenceIndex + 1).padStart(2, "0")}`], evidenceItemIds: [`evidence-${seed.code}-${String(evidenceIndex + 1).padStart(2, "0")}`], importance: index < evidenceCount - 1 ? "required" : "background", localizationKey: `fact.${seed.code}.${index + 1}` }; });
  const evidenceItems = Array.from({ length: evidenceCount }, (_, index) => ({ id: `evidence-${seed.code}-${String(index + 1).padStart(2, "0")}`, kind: index % 3 === 0 ? "scene" : index % 3 === 1 ? "record" : "object", titleKey: `evidence.${seed.code}.${index + 1}.title`, observationKey: `evidence.${seed.code}.${index + 1}.observation`, sourceFactIds: index === 0 ? facts.slice(0, 3).map((fact) => fact.id) : facts.filter((fact) => fact.evidenceItemIds?.[0] === `evidence-${seed.code}-${String(index + 1).padStart(2, "0")}`).map((fact) => fact.id), sourceEventIds: [events[index % events.length].id], relatedEntityIds: [`entity-${seed.code}-system`, `location-${seed.code}`], defaultState: index === 0 ? "discovered" : "available", supports: index === evidenceCount - 1 ? [] : ["hypothesis-canonical"], conflicts: index < 2 ? [`hypothesis-${seed.code}-alternative-${index + 1}`] : [], sourceLabelKey: index % 3 === 0 ? "evidence.source.scene" : index % 3 === 1 ? "evidence.source.record" : "evidence.source.object", importance: index === evidenceCount - 1 ? "irrelevant" : "required", visualAsset: `/assets/cases/${seed.code}/evidence-${String((index % 5) + 1).padStart(2, "0")}.svg`, assetAlt: seed.keywords[index % seed.keywords.length] }));
  const visibilityRules = [{ id: `rule-${seed.code}-opening`, mode: "scene", requires: { discoveredEvidenceIds: [], visitedLocationIds: [], answeredQueryIds: [] }, revealsFactIds: facts.slice(0, 3).map((fact) => fact.id), spoilerLevel: "safe" }, ...evidenceItems.map((item, index) => ({ id: `rule-${seed.code}-${String(index + 1).padStart(2, "0")}`, mode: "evidence", requires: { discoveredEvidenceIds: [item.id], visitedLocationIds: [], answeredQueryIds: [] }, revealsFactIds: item.sourceFactIds ?? [], spoilerLevel: index < 3 ? "safe" : "sensitive" }))];
  const questionSemantics = seed.keywords.map((keyword, index) => ({ id: `query-${seed.code}-${String(index + 1).padStart(2, "0")}`, labelKey: `query.${seed.code}.${index + 1}`, intent: index < evidenceCount - 1 ? "verify" : "eliminate", predicate: facts[index].predicate, slots: { subjectId: `entity-${seed.code}-system` }, answerCodeWhenVisible: index === seed.keywords.length - 1 ? "irrelevant" as const : "yes" as const, supportingFactIds: [facts[index].id], visibilityRuleIds: [index < 3 ? `rule-${seed.code}-opening` : `rule-${seed.code}-${String(index + 1).padStart(2, "0")}`], examplePhrases: [`${keyword}是否成立？`, `能确认${keyword}吗？`], matchRules: [{ all: [keyword], priority: 10 }, ...(index < 2 ? [{ any: ["关键记录"], priority: 2 }] : [])] }));
  const canonicalEvents = events.slice(0, -1).map((event) => event.id); const requiredEvidenceIds = evidenceItems.slice(0, -1).map((item) => item.id); const proofA = requiredEvidenceIds.slice(0, Math.min(8, requiredEvidenceIds.length)); const proofB = [...requiredEvidenceIds.slice(0, 2), ...requiredEvidenceIds.slice(Math.min(8, requiredEvidenceIds.length))];
  const reasoningBoards = seed.modes.map((mode, boardIndex) => { const indices = [boardIndex, Math.min(boardIndex + 2, canonicalEvents.length - 1), Math.min(boardIndex + 4, canonicalEvents.length - 1), canonicalEvents.length - 1]; const boardEvents = [...new Set(indices.map((index) => canonicalEvents[index]))]; return { id: `board-${seed.code}-${boardIndex + 1}`, titleKey: `board.${seed.code}.${boardIndex + 1}.title`, mode, slots: boardEvents.map((eventId, slotIndex) => ({ id: `slot-${seed.code}-${boardIndex + 1}-${slotIndex + 1}`, labelKey: `board.${seed.code}.${boardIndex + 1}.slot.${slotIndex + 1}`, acceptsEventIds: [eventId] })), allowedRelations: relations(mode) }; });
  const proofObligations = reasoningBoards.map((board) => ({ id: `obligation-${seed.code}-${board.id.split("-").pop()}`, kind: obligationKind(board.mode), boardId: board.id, requiredPlacements: board.slots.map((slot) => ({ slotId: slot.id, eventId: slot.acceptsEventIds[0] })), requiredConnections: board.slots.slice(0, -1).map((slot, index) => ({ fromEventId: slot.acceptsEventIds[0], toEventId: board.slots[index + 1].acceptsEventIds[0], relation: board.allowedRelations?.[0] ?? "explains" })), failureCategory: obligationKind(board.mode) }));
  const localization: Record<string, string> = { [`case.${seed.code}.title`]: seed.title, [`case.${seed.code}.surface`]: seed.surface, [`case.${seed.code}.truth`]: seed.truth, [`entity.${seed.code}.system`]: "测量系统与记录链", [`location.${seed.code}`]: "科学系统现场", [`hypothesis.${seed.code}.canonical`]: seed.truth, [`hypothesis.${seed.code}.alternative.1`]: seed.alternatives[0], [`hypothesis.${seed.code}.alternative.2`]: seed.alternatives[1], [`motive.${seed.purpose}`]: seed.purpose, [`chapter.${seed.code}.one`]: "固定异常", [`chapter.${seed.code}.two`]: "统一测量边界", "evidence.source.scene": "现场来源", "evidence.source.record": "记录来源", "evidence.source.object": "物证来源" };
  seed.keywords.forEach((keyword, index) => { localization[`query.${seed.code}.${index + 1}`] = `${keyword}是否成立？`; });
  events.forEach((_, index) => { localization[`event.${seed.code}.${index + 1}`] = `阶段 ${index + 1}：${seed.keywords[index % seed.keywords.length]}进入记录链`; });
  evidenceItems.forEach((_, index) => { localization[`evidence.${seed.code}.${index + 1}.title`] = `${seed.keywords[index % seed.keywords.length]}记录`; localization[`evidence.${seed.code}.${index + 1}.observation`] = `来源记录显示“${seed.keywords[index % seed.keywords.length]}”在该阶段保持可复核。`; });
  reasoningBoards.forEach((board, boardIndex) => { localization[`board.${seed.code}.${boardIndex + 1}.title`] = `${seed.title} · ${board.mode}`; board.slots.forEach((_, slotIndex) => { localization[`board.${seed.code}.${boardIndex + 1}.slot.${slotIndex + 1}`] = ["起始来源", "关键变化", "观测结果", "排除替代"][slotIndex] ?? `节点 ${slotIndex + 1}`; }); });
  const canonicalHash = `sha256:${createHash("sha256").update(JSON.stringify({ title: seed.title, truth: seed.truth, modes: seed.modes, keywords: seed.keywords })).digest("hex")}`;
  const replayCount = seed.code === "c60" ? 8 : 6;
  return { schemaVersion: "1.1", id, metadata: { titleKey: `case.${seed.code}.title`, difficulty: seed.code === "c60" ? "expert" : "advanced", ageRating: "16+", contentTags: seed.tags, targetMinutes: { min: seed.code === "c60" ? 15 : 10, max: seed.code === "c60" ? 25 : 20 }, contentVersion: 1, canonicalHash, status: "public-preview", seasonId: "season-4" }, presentation: { layoutId: `season4-${seed.modes[0]}`, sceneAsset: `/assets/cases/${seed.code}/scene.svg`, sceneAssetMobile: `/assets/cases/${seed.code}/scene-mobile.svg`, palette: `season4-${seed.code}`, accent: ["#b7d6a4", "#9bc7d8", "#d7aa76", "#c49bd8"][Number(seed.code.slice(1)) % 4], questionPromptMode: "forensic-host", evidenceVisualMode: `${seed.modes[0]}-lab`, mobileNavigation: ["现场", "记录", "推理板"], boardMode: seed.modes[0] }, surface: { titleKey: `case.${seed.code}.title`, textKey: `case.${seed.code}.surface`, publicFactIds: facts.slice(0, 3).map((fact) => fact.id), initialQuestionPrompts: questionSemantics.slice(0, 3).map((query) => query.id) }, chapters: seed.code === "c60" ? [{ id: `chapter-${seed.code}-one`, titleKey: `chapter.${seed.code}.one`, unlock: {} }, { id: `chapter-${seed.code}-two`, titleKey: `chapter.${seed.code}.two`, unlock: { discoveredEvidenceIds: evidenceItems.slice(0, 6).map((item) => item.id) } }] : [{ id: `chapter-${seed.code}-one`, titleKey: `chapter.${seed.code}.one`, unlock: {} }], reasoningBoards, replayChallenges: [{ mode: "limited-questions", questionLimit: 14 }, { mode: "minimal-proof" }, { mode: "no-scaffolds" }], entities: [{ id: `entity-${seed.code}-system`, kind: "object", labelKey: `entity.${seed.code}.system`, publicAtStart: true }, { id: `location-${seed.code}`, kind: "location", labelKey: `location.${seed.code}`, publicAtStart: true }], events, relations: reasoningBoards.flatMap((board, boardIndex) => board.slots.slice(0, -1).map((slot, index) => ({ id: `relation-${seed.code}-${boardIndex + 1}-${index + 1}`, type: index === 0 ? "precedes" : "causes", fromId: `entity-${seed.code}-system`, toId: `location-${seed.code}`, eventId: slot.acceptsEventIds[0], factId: facts[Math.min(index + boardIndex, facts.length - 1)].id }))), facts, visibilityRules, questionSemantics, answerPolicy: { codes: ["yes", "no", "partial", "invalid_premise", "unknown", "irrelevant", "unanswerable", "unrecognized"], templates: { yes: "是。当前公开来源支持这个判断。", no: "不是。这个判断与已验证约束冲突。", partial: "部分相关，但因果链还没有闭合。", invalid_premise: "这个问题的前提不成立。", unknown: "目前还没有足够证据判断。", irrelevant: "这条信息真实，但不会改变核心因果链。", unanswerable: "案件没有定义这个属性。", unrecognized: "我还不能把问题对应到明确的调查事实。" } }, evidenceItems, hypotheses: [{ id: "hypothesis-canonical", kind: "canonical", labelKey: `hypothesis.${seed.code}.canonical`, claim: { eventIds: canonicalEvents, motiveKey: seed.purpose }, requiredEvidenceIds, requiredContradictionResolutionIds: [`contradiction-${seed.code}-1`, `contradiction-${seed.code}-2`], status: "candidate" }, { id: `hypothesis-${seed.code}-alternative-1`, kind: "alternative", labelKey: `hypothesis.${seed.code}.alternative.1`, claim: { assertedPropositions: [seed.alternatives[0]], motiveKey: `motive-${seed.code}-alternative-1` }, requiredEvidenceIds: [requiredEvidenceIds[0]], requiredContradictionResolutionIds: [`contradiction-${seed.code}-1`], status: "candidate" }, { id: `hypothesis-${seed.code}-alternative-2`, kind: "alternative", labelKey: `hypothesis.${seed.code}.alternative.2`, claim: { assertedPropositions: [seed.alternatives[1]], motiveKey: `motive-${seed.code}-alternative-2` }, requiredEvidenceIds: [requiredEvidenceIds[1]], requiredContradictionResolutionIds: [`contradiction-${seed.code}-2`], status: "candidate" }], contradictions: [{ id: `contradiction-${seed.code}-1`, kind: "alternative-exclusion", requiresFactIds: [facts[0].id], invalidatesHypothesisIds: [`hypothesis-${seed.code}-alternative-1`], resolutionEvidenceIds: [requiredEvidenceIds[0]], severity: "hard" }, { id: `contradiction-${seed.code}-2`, kind: "alternative-exclusion", requiresFactIds: [facts[1].id], invalidatesHypothesisIds: [`hypothesis-${seed.code}-alternative-2`], resolutionEvidenceIds: [requiredEvidenceIds[1]], severity: "hard" }], solutionCertificate: { canonicalHypothesisId: "hypothesis-canonical", requiredFactIds: facts.slice(0, requiredEvidenceIds.length).map((fact) => fact.id), requiredEvidenceIds, requiredEventOrder: canonicalEvents.slice(0, -1).map((eventId, index) => [eventId, canonicalEvents[index + 1]]), requiredContradictionResolutionIds: [`contradiction-${seed.code}-1`, `contradiction-${seed.code}-2`], alternativeHypothesisIds: [`hypothesis-${seed.code}-alternative-1`, `hypothesis-${seed.code}-alternative-2`], minimumProofSetId: `proof-${seed.code}-a`, minimumProofSets: [{ id: `proof-${seed.code}-a`, evidenceIds: proofA }, { id: `proof-${seed.code}-b`, evidenceIds: proofB }], acceptedMotiveKeys: [seed.purpose], proofObligations, uniquenessClaim: seed.truth }, proofReplay: Array.from({ length: replayCount }, (_, index) => ({ id: `replay-${seed.code}-${index + 1}`, eventId: canonicalEvents[Math.min(index + 1, canonicalEvents.length - 1)], factIds: index === replayCount - 1 ? facts.slice(0, requiredEvidenceIds.length).map((fact) => fact.id) : [facts[index % facts.length].id], captionKey: `event.${seed.code}.${Math.min(index + 2, eventCount)}` })), localization: { "zh-CN": localization } } as CaseFile;
}

for (const seed of specs) {
  const file = `${caseId(seed)}.json`; const caseFile = makeCase(seed); const questions = corpus(seed); write(resolve(caseDir, file), caseFile); write(resolve(caseDir, `${seed.code}-question-corpus.ts`), `import type { QueryCorpusEntry } from "../../../packages/mystery-core/src/index.ts";\n\nexport const ${seed.code}QuestionCorpus: QueryCorpusEntry[] = ${JSON.stringify(questions, null, 2)};\n`); write(resolve(caseDir, `${caseId(seed)}.test-vectors.json`), { caseId: caseId(seed), normalizationVectors: questions.slice(0, 24), ambiguityVectors: questions.filter((item) => item.expectedStatus === "ambiguous"), failClosedVectors: questions.filter((item) => item.expectedStatus === "unrecognized"), canonical: { evidenceIds: caseFile.solutionCertificate.minimumProofSets[0].evidenceIds, alternativeEvidenceIds: caseFile.solutionCertificate.minimumProofSets[1].evidenceIds, eventCount: caseFile.events.length, boardModes: seed.modes } }); write(resolve(root, "docs", `${caseId(seed)}-content-review.md`), `# ${seed.title} 内容审查\n\n- 状态：public-preview / human-evaluation-pending\n- 核心机制：${seed.truth}\n- 推理板：${seed.modes.join(" + ")}\n- 事件 / 事实 / 证据：${caseFile.events.length} / ${caseFile.facts.length} / ${caseFile.evidenceItems.length}\n- 最小证明集合：${caseFile.solutionCertificate.minimumProofSets.length}\n- 语料：${questions.length} 条；预期歧义 8 条；真人参与：0。\n`);
  const codeDir = resolve(assetRoot, seed.code); mkdirSync(codeDir, { recursive: true }); const accent = caseFile.presentation?.accent ?? "#9bc7d8"; const scene = `<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1200 680"><rect width="1200" height="680" fill="#0b1116"/><path d="M0 520h1200" stroke="${accent}" stroke-width="2" opacity=".7"/><g fill="none" stroke="${accent}" opacity=".55"><rect x="100" y="110" width="1000" height="360" rx="18"/><path d="M180 400h840M260 160v260M540 160v260M820 160v260"/></g><circle cx="600" cy="300" r="90" fill="${accent}" opacity=".12"/><text x="600" y="560" fill="#e8e2d1" text-anchor="middle" font-family="serif" font-size="34">${seed.code.toUpperCase()} · ${seed.title}</text></svg>`; const mobile = scene.replace("viewBox=\"0 0 1200 680\"", "viewBox=\"0 0 760 1000\""); writeFileSync(resolve(codeDir, "scene.svg"), scene, "utf8"); writeFileSync(resolve(codeDir, "scene-mobile.svg"), mobile, "utf8"); for (let index = 1; index <= 5; index += 1) writeFileSync(resolve(codeDir, `evidence-${String(index).padStart(2, "0")}.svg`), `<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 640 400"><rect width="640" height="400" fill="#121a20"/><path d="M80 300h480M120 100v200M520 100v200" stroke="${accent}" stroke-width="5" opacity=".7"/><text x="320" y="220" fill="#e8e2d1" text-anchor="middle" font-family="sans-serif" font-size="28">${seed.code.toUpperCase()} / E${index}</text></svg>`, "utf8");
}

const manifestCases = specs.map((seed) => { const file = makeCase(seed); return { id: caseId(seed), file: `${caseId(seed)}.json`, contentVersion: 1, canonicalHash: file.metadata?.canonicalHash, status: "public-preview", publishable: false, public: { title: seed.title, surface: seed.surface, difficulty: file.metadata?.difficulty, targetMinutes: file.metadata?.targetMinutes, contentTags: seed.tags, layoutId: file.presentation?.layoutId, sceneAsset: file.presentation?.sceneAsset, sceneAssetMobile: file.presentation?.sceneAssetMobile, accent: file.presentation?.accent, boardModes: seed.modes, replayChallenges: file.replayChallenges } }; });
write(resolve(caseDir, "manifest.season4.v1.3.json"), { manifestVersion: "1.3", seasonId: "season-4", title: "第四季：测量之外", status: "public-preview", publishable: false, humanEvaluation: { status: "pending", participants: 0, requiredBeforePublish: true }, cases: manifestCases });
write(resolve(root, "content/zh/releases/v1.3-public-preview.json"), { profileVersion: 1, id: "v1.3-public-preview", title: "TURTLE SOUP v1.3 Public Preview", status: "public-preview", publishable: false, humanEvaluation: "pending", manifests: ["../cases/manifest.v0.6.json", "../cases/manifest.season2.v1.0.json", "../cases/manifest.season3.v1.1.json", "../cases/manifest.season4.v1.3.json"] });
console.log(JSON.stringify({ generatedCases: specs.length, corpusEntries: specs.reduce((sum, seed) => sum + corpus(seed).length, 0), manifest: "content/zh/cases/manifest.season4.v1.3.json" }, null, 2));
