import { existsSync, readFileSync, readdirSync, statSync, writeFileSync } from "node:fs";
import { extname, resolve } from "node:path";
import { loadCaseFile, loadReleaseContent } from "./lib/release-content.ts";
const root = resolve(process.argv[2] ?? "."); const entries = loadReleaseContent(root, "v1.5-internal-rc").entries; const cases = entries.map(loadCaseFile); const hidden = cases.flatMap((file) => [file.solutionCertificate.canonicalHypothesisId, ...file.facts.map((fact) => fact.id), ...file.events.map((event) => event.id), ...(file.solutionCertificate.proofObligations ?? []).map((item) => item.id)]);
function files(dir: string): string[] { return readdirSync(dir).flatMap((name) => { const path = resolve(dir, name); return statSync(path).isDirectory() ? files(path) : [path]; }); } const output = resolve(root, "apps/web/out"); const texts = existsSync(output) ? files(output).filter((path) => [".html", ".js", ".json", ".css", ".txt", ".webmanifest"].includes(extname(path).toLowerCase())).map((path) => ({ path, text: readFileSync(path, "utf8") })) : [];
const truthBearingTextFiles = texts.filter(({ text }) => text.includes("solutionCertificate") || hidden.some((id) => id && text.includes(id))).length; const sourceMaps = existsSync(output) ? files(output).filter((path) => path.endsWith(".map")).length : 0; const remoteReferences = texts.filter(({ text }) => /api\.openai\.com|google-analytics|segment\.io|mixpanel|sentry\.io|plausible\.io/i.test(text)).length; const diagnosticsLeak = texts.filter(({ text }) => /rawQuestion|commandLog|canonicalHypothesisId|solutionCertificate|hiddenFact/i.test(text)).length; const publicHtmlLeaks = texts.filter(({ path, text }) => path.endsWith(".html") && hidden.some((id) => id && text.includes(id))).map((item) => item.path); const historySource = readFileSync(resolve(root, "apps/web/lib/golden-experience.ts"), "utf8");
const failures = [truthBearingTextFiles ? "" : "no truth-bearing worker chunk", sourceMaps ? `${sourceMaps} source maps shipped` : "", remoteReferences ? `${remoteReferences} remote references found` : "", diagnosticsLeak > truthBearingTextFiles ? `diagnostic/public leak in ${diagnosticsLeak} files` : "", publicHtmlLeaks.length ? `${publicHtmlLeaks.length} public HTML truth leaks` : "", /solutionCertificate|canonicalHypothesis|fact-[\w-]+|event-internal/i.test(historySource) ? "golden presentation config contains truth identifiers" : ""].filter(Boolean);
const report = { reportVersion: "1.5", releaseProfile: "v1.5-internal-rc", generatedAt: new Date().toISOString(), status: "internal-rc / human-evaluation-pending", humanParticipants: 0, hiddenIdentifierCount: hidden.length, targets: [{ name: "static-output", files: texts.length, truthBearingTextFiles, sourceMaps, remoteReferences, diagnosticsLeak, publicHtmlLeaks, failures, passed: failures.length === 0 }], boundaries: { fullCaseFile: "worker-only", questionAliases: "worker-only", saveSchemaVersion: 1, remoteAnalytics: false, remoteAI: false, localHostRewriteDefault: "off" }, passed: failures.length === 0, qualification: "Static scans reduce accidental publication leakage; offline content remains reverse-engineerable and this is not DRM." }; writeFileSync(resolve(root, "docs/v1.5-security-scan.json"), `${JSON.stringify(report, null, 2)}\n`, "utf8"); console.log(JSON.stringify({ files: texts.length, truthBearingTextFiles, publicHtmlLeaks: publicHtmlLeaks.length, remoteReferences, passed: report.passed }, null, 2)); if (!report.passed) process.exitCode = 1;
