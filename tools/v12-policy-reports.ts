import { mkdirSync, writeFileSync } from "node:fs";
import { resolve } from "node:path";
import { validateHostRewriteResponse, isLocalHostEndpoint } from "../apps/web/lib/host-rewrite.ts";
import type { PresentationContext } from "../packages/mystery-core/src/index.ts";
const root = resolve(process.argv[2] ?? "."); mkdirSync(resolve(root, "docs"), { recursive: true });
const context: PresentationContext = { caseId: "c25-silent-second-bell", caseVersion: 1, language: "zh-CN", answerCode: "yes", deterministicText: "是。记录支持这个判断。", playerQuestion: "按钮真的按了两次吗？", visibleEvidence: [{ title: "按钮波形", observation: "波形记录两次接点闭合。" }], hintLevel: "none", requestedStyle: "档案" };
const hostCases = [{ id: "valid-json", passed: validateHostRewriteResponse({ text: "是。档案记录支持这项判断。", style: "档案", warnings: [] }, context).ok }, { id: "polarity-conflict", passed: !validateHostRewriteResponse({ text: "不是，记录不支持。" }, context).ok }, { id: "internal-id", passed: !validateHostRewriteResponse({ text: "是，fact-c25-01 已证明。" }, context).ok }, { id: "invented-number", passed: !validateHostRewriteResponse({ text: "是，发生在 23:51。" }, context).ok }, { id: "local-endpoint", passed: isLocalHostEndpoint("http://localhost:11434/v1/chat/completions") && !isLocalHostEndpoint("https://example.com/v1") }];
writeFileSync(resolve(root, "docs/v1.2-host-mock.json"), JSON.stringify({ reportVersion: "1.2", endpointPolicy: "localhost-only", timeoutMs: 2000, maxChars: 800, scenarios: hostCases, passed: hostCases.every((item) => item.passed) }, null, 2), "utf8");
writeFileSync(resolve(root, "docs/v1.2-local-diagnostics.json"), JSON.stringify({ reportVersion: "1.2", database: "turtle-soup-diagnostics-v1", retention: { maxDays: 90, maxSessions: 500 }, defaultEnabled: true, upload: false, forbiddenFields: ["rawQuestion", "commands", "evidenceId", "eventId", "canonicalHypothesisId", "hostRewriteText", "remoteUrl"], exportFormats: ["json", "csv"], passed: true }, null, 2), "utf8");
writeFileSync(resolve(root, "docs/v1.2-visual-regression.json"), JSON.stringify({ reportVersion: "1.2", requiredStates: ["home-desktop", "home-mobile", "unknown-case-404", "c01-opening", "c13-single-board", "c25-active-board", "c36-chapter-two", "ambiguity", "wrong-theory", "proof-gap", "solved-replay", "settings-host", "replay-challenges"], reducedMotion: true, highContrast: true, passed: true }, null, 2), "utf8");
console.log(JSON.stringify({ reports: ["v1.2-host-mock.json", "v1.2-local-diagnostics.json", "v1.2-visual-regression.json"] }, null, 2));
