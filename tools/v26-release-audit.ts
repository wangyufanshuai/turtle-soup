import { createHash } from "node:crypto";
import { existsSync, readFileSync, writeFileSync } from "node:fs";
import { resolve } from "node:path";
import { validateCaseShape } from "../packages/mystery-core/src/index.ts";
import { loadCaseFile, loadReleaseContent } from "./lib/release-content.ts";
const root = resolve(process.argv[2] ?? ".");
const profileId = "v2.6-internal-rc";
const current = loadReleaseContent(root, profileId); const baseline = loadReleaseContent(root, "v2.5-internal-rc");
const frozen = baseline.entries.map((entry) => { const next = current.entries.find((item) => item.id === entry.id); return { caseId: entry.id, canonicalHashUnchanged: next?.canonicalHash === entry.canonicalHash, contentVersionUnchanged: next?.contentVersion === entry.contentVersion, casePathUnchanged: next?.casePath === entry.casePath }; });
const cases = current.entries.map((entry) => { try { const file = loadCaseFile(entry); validateCaseShape(file); return { caseId: entry.id, schemaValid: true, events: file.events.length, facts: file.facts.length, evidence: file.evidenceItems.length, boards: file.reasoningBoards?.map((item) => item.mode) ?? [], replayBeats: file.proofReplay.length }; } catch (error) { return { caseId: entry.id, schemaValid: false, error: error instanceof Error ? error.message : String(error) }; } });
const required = ["docs/v2.6-precache.json", "docs/v2.6-browser-matrix.json", "docs/v2.6-visual-regression.json", "docs/v2.6-static-performance.json", "docs/v2.6-security-scan.json", "docs/v2.6-save-compatibility.json", "docs/v2.6-experience-audit.json", "docs/v2.6-content-difference.json", "docs/v2.6-release-artifacts.json", "docs/v2.6-internal-rc.md", "dist/turtle-soup-v2.6-internal-rc-web-pwa.zip"];
const requiredFiles = required.map((path) => ({ path, exists: existsSync(resolve(root, path)) }));
const reports = required.filter((path) => path.endsWith(".json") && path !== "docs/v2.6-precache.json").map((path) => { try { const value = JSON.parse(readFileSync(resolve(root, path), "utf8")) as { passed?: boolean }; return { path, passed: value.passed === true }; } catch { return { path, passed: false }; } });
const historical = [{ version: "v1.8", file: "dist/turtle-soup-v1.8-internal-rc-web-pwa.zip", expected: "43b6dff6b70abec6a74788bb28fba6f7301eb91dd892818584c82f48fc88d30d" }, { version: "v1.9", file: "dist/turtle-soup-v1.9-internal-rc-web-pwa.zip", expected: "c3c074e1170ca2efd7db0b3c0437873dc7d8633074ab4ff4c62c5b7910a8a6fc" }, { version: "v2.0", file: "dist/turtle-soup-v2.0-internal-rc-web-pwa.zip", expected: "76e8d237acdc94f99f8cf48ca25bc8f1b22b6bdc86ce2a74153700d290a27273" }, { version: "v2.1", file: "dist/turtle-soup-v2.1-internal-rc-web-pwa.zip", expected: "af82f2ea2c7c29f4d934726afd96be5319b68693dcb9ed61747d36e784784929" }, { version: "v2.2", file: "dist/turtle-soup-v2.2-internal-rc-web-pwa.zip", expected: "730a4364fbde1520214f00fc9afdf5f6605259359e695498486e83da35b6b937" }, { version: "v2.3", file: "dist/turtle-soup-v2.3-internal-rc-web-pwa.zip", expected: "e88a0b12d9437cd3c9e3d94bce81481756629ae19439cfd93ed406f45586f9d4" }, { version: "v2.4", file: "dist/turtle-soup-v2.4-internal-rc-web-pwa.zip", expected: "58b4b4f140cbcd959b3578037cbe94894491204d03cbf923ca648f1f927db25f" }, { version: "v2.5", file: "dist/turtle-soup-v2.5-internal-rc-web-pwa.zip", expected: "78d1a2b3168a729895734c5858c0415c459661f75ab8445bfaf14c0d514d5eaf" }].map((item) => { const path = resolve(root, item.file); const actual = existsSync(path) ? createHash("sha256").update(readFileSync(path)).digest("hex") : "missing"; return { ...item, actual, unchanged: actual === item.expected }; });
const profile = current.profile;
const gates = { profile: profile.id === profileId && profile.status === "internal-rc" && profile.publishable === false && profile.humanEvaluation === "pending", participants: profile.formalFunGateParticipants === 0, caseCount: current.entries.length === 84, frozen: frozen.length === 84 && frozen.every((item) => item.canonicalHashUnchanged && item.contentVersionUnchanged && item.casePathUnchanged), schema: cases.length === 84 && cases.every((item) => item.schemaValid), reports: requiredFiles.every((item) => item.exists) && reports.every((item) => item.passed), historicalArtifacts: historical.every((item) => item.unchanged) };
const report = { reportVersion: "2.6", releaseProfile: profileId, generatedAt: new Date().toISOString(), status: "internal-rc / human-evaluation-pending", humanParticipants: 0, founderExploratorySessions: 1, gates, frozen, cases, requiredFiles, reports, historicalArtifacts: historical, passed: Object.values(gates).every(Boolean), qualification: "v2.6 hardens the investigation workbench and navigation while preserving frozen case truth, hashes, proof certificates and schema-one saves. Automation does not establish fun, comprehension or market fit." };
writeFileSync(resolve(root, "docs/v2.6-release-verification.json"), `${JSON.stringify(report, null, 2)}\n`, "utf8");
console.log(JSON.stringify({ caseCount: current.entries.length, gates, passed: report.passed }, null, 2));
if (!report.passed) process.exitCode = 1;
