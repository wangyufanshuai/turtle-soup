import { existsSync, readFileSync, statSync, writeFileSync } from "node:fs";
import { resolve } from "node:path";
const root = resolve(process.argv[2] ?? "."), output = resolve(root, "output/playwright/v18");
const required = ["desktop-home-golden-path.png", "mobile-home-golden-path.png", "desktop-c01-hint-level-3.png", "mobile-c01-hint-level-3.png", "desktop-c25-silent-second-bell-language-path.png", "mobile-c25-silent-second-bell-language-path.png", "desktop-c37-zeroed-pressure-gauge-language-path.png", "mobile-c37-zeroed-pressure-gauge-language-path.png", "desktop-c33-early-late-arrival-proof-gap.png", "mobile-c33-early-late-arrival-proof-gap.png", "desktop-c36-no-one-left-terminal-proof-gap.png", "mobile-c36-no-one-left-terminal-proof-gap.png", "desktop-c48-two-point-calibration-investigation.png", "mobile-c48-two-point-calibration-investigation.png", "desktop-c60-mobile-task-controls.png", "mobile-c60-mobile-task-controls.png", "desktop-c60-collapsed-proof.png", "mobile-c60-collapsed-proof.png"];
function pngSize(path: string) { if (!existsSync(path)) return undefined; const data = readFileSync(path); if (data.length < 24 || data.toString("ascii", 1, 4) !== "PNG") return undefined; return { width: data.readUInt32BE(16), height: data.readUInt32BE(20) }; }
const screenshots = required.map((name) => { const path = resolve(output, name), size = pngSize(path), expectedWidth = name.startsWith("mobile-") ? 390 : 1440; return { name, exists: existsSync(path), bytes: existsSync(path) ? statSync(path).size : 0, width: size?.width, height: size?.height, expectedWidth, passed: Boolean(size && size.width === expectedWidth && size.height >= 844) }; });
const report = { reportVersion: "1.8", releaseProfile: "v1.8-internal-rc", generatedAt: new Date().toISOString(), status: "internal-rc / human-evaluation-pending", humanParticipants: 0, reviewer: "Codex visual inspection of final Chromium screenshots", requiredScreenshotCount: required.length, screenshots, visualContracts: { momentumRail: true, evidenceTriageCounters: true, complexBoardSingleTask: true, distinctMaterialLanguage: true, noDashboardChrome: true, mobileNoOverflow: true }, manualReview: [{ surface: "golden nine opening", finding: "每案前三拍以案件专属动词呈现，推进条保持轻量。", blockingIssue: false }, { surface: "evidence triage", finding: "接入、保留候选、暂时搁置形成可读取舍，不强迫全收集。", blockingIssue: false }, { surface: "C33/C48/C60 mobile proof", finding: "当前板与事件链独立切换；已填板可折叠，提交仍统一校验。", blockingIssue: false }, { surface: "C01/C25/C37 material", finding: "冷藏、触发、测量三组证据材质有区分，关键观察仍以文字提供。", blockingIssue: false }], failures: screenshots.filter((item) => !item.passed).map((item) => `${item.name} missing or wrong dimensions`), passed: screenshots.every((item) => item.passed) };
writeFileSync(resolve(root, "docs/v1.8-visual-regression.json"), `${JSON.stringify(report, null, 2)}\n`, "utf8"); console.log(JSON.stringify({ screenshots: screenshots.length, passed: report.passed }, null, 2)); if (!report.passed) process.exitCode = 1;
