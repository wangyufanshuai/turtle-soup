import { readFileSync, readdirSync, statSync, writeFileSync } from "node:fs";
import { extname, resolve } from "node:path";
const root = resolve(process.argv[2] ?? "."); const outDir = resolve(root, "apps/web/out");
function files(dir: string): string[] { return readdirSync(dir).flatMap((name) => { const path = resolve(dir, name); return statSync(path).isDirectory() ? files(path) : [path]; }); }
const all = files(outDir).map((path) => ({ path, relative: path.slice(outDir.length + 1).replaceAll("\\", "/"), ext: extname(path).toLowerCase(), bytes: statSync(path).size })); const index = readFileSync(resolve(outDir, "index.html"), "utf8"); const initial = [...index.matchAll(/<script[^>]+src="([^"]+)"/g)].map((match) => match[1].replace(/^\//, "")); const sum = (ext: string) => all.filter((item) => item.ext === ext).reduce((total, item) => total + item.bytes, 0); const actual = { totalBytes: all.reduce((total, item) => total + item.bytes, 0), initialJavaScriptBytes: all.filter((item) => initial.some((script) => item.relative.endsWith(script))).reduce((total, item) => total + item.bytes, 0), maximumAssetBytes: Math.max(...all.map((item) => item.bytes)), sourceMaps: all.filter((item) => item.ext === ".map").length, javascriptBytes: sum(".js") }; const budgets = { totalBytes: 8_000_000, initialJavaScriptBytes: 1_400_000, maximumAssetBytes: 450_000, sourceMaps: 0 }; const sw = readFileSync(resolve(outDir, "sw.js"), "utf8"); const failures = Object.entries(budgets).flatMap(([key, value]) => actual[key as keyof typeof actual] <= value ? [] : [`${key}: ${actual[key as keyof typeof actual]} > ${value}`]); if (!sw.includes("black-soup-v13-preview-1")) failures.push("v1.3 cache identity missing"); const report = { reportVersion: "1.3", releaseProfile: "v1.3-public-preview", generatedAt: new Date().toISOString(), budgets, actual, failures, passed: failures.length === 0 }; writeFileSync(resolve(root, "docs/v1.3-static-performance.json"), JSON.stringify(report, null, 2), "utf8"); console.log(JSON.stringify(report, null, 2)); if (!report.passed) process.exitCode = 1;
