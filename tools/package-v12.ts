import { createHash } from "node:crypto"; import { execFileSync } from "node:child_process"; import { cpSync, existsSync, mkdirSync, readdirSync, rmSync, statSync, readFileSync, writeFileSync } from "node:fs"; import { relative, resolve, sep } from "node:path";
const root = resolve(process.argv[2] ?? "."); const source = resolve(root, "apps/web/out"); const dist = resolve(root, "dist/v1.2-internal-rc-web-pwa"); const archive = resolve(root, "dist/turtle-soup-v1.2-internal-rc-web-pwa.zip"); if (!existsSync(source)) throw new Error("Run npm run build first"); rmSync(dist, { recursive: true, force: true }); mkdirSync(dist, { recursive: true }); cpSync(source, dist, { recursive: true }); if (existsSync(archive)) rmSync(archive); const quote = (value: string) => value.replaceAll("'", "''"); execFileSync("powershell.exe", ["-NoProfile", "-Command", `Compress-Archive -Path '${quote(dist)}\\*' -DestinationPath '${quote(archive)}' -CompressionLevel Optimal -Force`], { stdio: "inherit" }); function files(dir: string): string[] { return readdirSync(dir).flatMap((name) => { const path = resolve(dir, name); return statSync(path).isDirectory() ? files(path) : [path]; }); } const digest = createHash("sha256"); for (const path of files(dist).sort()) { digest.update(relative(dist, path).split(sep).join("/")); digest.update(readFileSync(path)); } const report = { reportVersion: "1.2", releaseProfile: "v1.2-internal-rc", generatedAt: new Date().toISOString(), humanParticipants: 0, humanFunGate: "pending", status: "internal-rc / human-evaluation-pending", artifact: { directory: "dist/v1.2-internal-rc-web-pwa", bytes: files(dist).reduce((sum, path) => sum + statSync(path).size, 0), files: files(dist).length, sha256: digest.digest("hex"), archive: { path: "dist/turtle-soup-v1.2-internal-rc-web-pwa.zip", bytes: statSync(archive).size, sha256: createHash("sha256").update(readFileSync(archive)).digest("hex") } }, passed: true }; writeFileSync(resolve(root, "docs/v1.2-release-artifacts.json"), JSON.stringify(report, null, 2), "utf8"); console.log(JSON.stringify(report, null, 2));
