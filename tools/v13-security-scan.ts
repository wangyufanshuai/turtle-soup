import { existsSync, readFileSync, readdirSync, statSync, writeFileSync } from "node:fs"; import { extname, resolve } from "node:path"; import { loadCaseFile, loadReleaseContent } from "./lib/release-content.ts";
const root = resolve(process.argv[2] ?? "."); const entries = loadReleaseContent(root, "v1.3-public-preview").entries; const cases = entries.map(loadCaseFile); const hidden = cases.flatMap((file) => [file.solutionCertificate.canonicalHypothesisId, ...file.facts.map((fact) => fact.id), ...file.events.map((event) => event.id), ...(file.solutionCertificate.proofObligations ?? []).map((item) => item.id)]); function files(dir: string): string[] { return readdirSync(dir).flatMap((name) => { const path = resolve(dir, name); return statSync(path).isDirectory() ? files(path) : [path]; }); } const output = resolve(root, "apps/web/out"); const texts = existsSync(output) ? files(output).filter((path) => [".html", ".js", ".json", ".css", ".txt", ".webmanifest"].includes(extname(path).toLowerCase())).map((path) => readFileSync(path, "utf8")) : []; const truthBearingTextFiles = texts.filter((text) => text.includes("solutionCertificate") || hidden.some((id) => text.includes(id))).length; const sourceMaps = existsSync(output) ? files(output).filter((path) => path.endsWith(".map")).length : 0; const remoteReferences = texts.filter((text) => /api\.openai\.com|google-analytics|segment\.io|mixpanel|sentry\.io/i.test(text)).length; const failures = [truthBearingTextFiles ? "" : "no truth-bearing worker chunk", sourceMaps ? `${sourceMaps} source maps shipped` : "", remoteReferences ? `${remoteReferences} remote references found` : ""].filter(Boolean); const report = { reportVersion: "1.3", releaseProfile: "v1.3-public-preview", generatedAt: new Date().toISOString(), hiddenIdentifierCount: hidden.length, targets: [{ name: "static-output", files: texts.length, truthBearingTextFiles, sourceMaps, remoteReferences, failures, passed: failures.length === 0 }], passed: failures.length === 0 }; writeFileSync(resolve(root, "docs/v1.3-security-scan.json"), JSON.stringify(report, null, 2), "utf8"); console.log(JSON.stringify(report, null, 2)); if (!report.passed) process.exitCode = 1;
