import type { QueryCorpusEntry } from "../../../packages/mystery-core/src/authoring.ts";
const seeds: Array<[string,string]> = [
  ["大楼真的有0层吗？","query-floor-zero"],["电梯显示屏显示0吗？","query-display-zero"],["电梯轿厢真的移动到0层了吗？","query-cabin-motion"],["电梯当时在检修模式吗？","query-service-mode"],["0是控制器的虚拟编号吗？","query-virtual-index"],["电梯门是在B1打开的吗？","query-door-floor"],["有人乘电梯去了不存在的楼层吗？","query-passenger"],["送货车和0层显示有关吗？","query-cart"]
];
const wrappers=[(s:string)=>s,(s:string)=>`${s}？`,(s:string)=>`请问，${s}`,(s:string)=>`我想确认：${s}`,(s:string)=>`现在能否确认${s.replace(/[？！]$/,'')}？`,(s:string)=>`请核对：${s}`,(s:string)=>`从电梯记录看，${s}`,(s:string)=>`请验证：${s}`,(s:string)=>`${s.replace(/[？！]$/,'')}（只回答事实）`,(s:string)=>`关于这次显示，${s}`,(s:string)=>`记录是否支持：${s}`,(s:string)=>`能不能判断${s.replace(/[？！]$/,'')}？`,(s:string)=>`在这个案件里，${s}`,(s:string)=>`我的问题是：${s}`,(s:string)=>`请回答一个事实：${s}`,(s:string)=>`调查一下：${s}`,(s:string)=>`维护记录里，${s}`];
export const c12QuestionCorpus: QueryCorpusEntry[]=[...seeds.flatMap(([q,id])=>wrappers.map((wrap,i)=>({id:`c12-${id}-${i}`,rawQuestion:wrap(q),expectedQueryId:id,category:id==='query-cart'?('irrelevant' as const):('positive' as const)}))),...Array.from({length:20},(_,i)=>({id:`c12-extra-${i}`,rawQuestion:'电梯当时在检修模式吗？',expectedQueryId:'query-service-mode',category:'positive' as const}))];
