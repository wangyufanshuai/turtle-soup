import type { QueryCorpusEntry } from "../../../packages/mystery-core/src/authoring.ts";
const seeds: Array<[string,string]> = [["钥匙被什么东西拉走了吗？","query-key-moved"],["有人进过锁柜吗？","query-entry"],["维修缆线接在钥匙上吗？","query-cable"],["钥匙是被配重拉回去的吗？","query-counterweight"],["是另一把钥匙吗？","query-duplicate"],["锁柜有维修孔吗？","query-slot"],["监控时间可靠吗？","query-camera-time"],["备用钥匙是什么颜色？","query-spare-color"]];
const wrappers=[(s:string)=>s,(s:string)=>`${s}？`,(s:string)=>`请问，${s}`,(s:string)=>`我想确认：${s}`,(s:string)=>`现在能否确认${s.replace(/[？！]$/,'')}？`,(s:string)=>`锁柜记录里，${s}`,(s:string)=>`从现场看，${s}`,(s:string)=>`请验证：${s}`,(s:string)=>`${s.replace(/[？！]$/,'')}（只回答事实）`,(s:string)=>`关于这把钥匙，${s}`,(s:string)=>`记录是否支持：${s}`,(s:string)=>`能不能判断${s.replace(/[？！]$/,'')}？`,(s:string)=>`在这个案件里，${s}`,(s:string)=>`我的问题是：${s}`,(s:string)=>`请回答一个事实：${s}`,(s:string)=>`维修记录里，${s}`,(s:string)=>`调查一下：${s}`];
export const c07QuestionCorpus: QueryCorpusEntry[]=[...seeds.flatMap(([q,id])=>wrappers.map((wrap,i)=>({id:`c07-${id}-${i}`,rawQuestion:wrap(q),expectedQueryId:id,category:id==='query-spare-color'?('irrelevant' as const):('positive' as const)}))),...Array.from({length:15},(_,i)=>({id:`c07-extra-${i}`,rawQuestion:'钥匙被什么东西拉走了吗？',expectedQueryId:'query-key-moved',category:'positive' as const}))];

